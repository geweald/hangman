import React from 'react';

const Corpus = ({ hidden }) => (
  <div className="Corpus" hidden={hidden}>
    <div className="Corpus__left"></div>
    <div className="Corpus__bottom"></div>
  </div>
);

export default Corpus;